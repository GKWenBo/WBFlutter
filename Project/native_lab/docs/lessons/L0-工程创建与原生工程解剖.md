# L0 工程创建与原生工程解剖

> 企业场景：入职第一天，接手一个 Flutter 工程，你得先看懂 `ios/` 和 `android/` 到底是什么、Dart 代码是怎么跑进手机里的——这决定了你后面九课的所有原生交互代码往哪写。

## 一、本课要掌握什么

**1. `ios/` 就是一个完整的 Xcode 工程，`android/` 就是一个完整的 Gradle 工程。**

`flutter create` 没有魔法：它生成的 `ios/Runner.xcworkspace` 你可以直接用 Xcode 打开、编译、断点调试，和你写了多年的原生工程没有本质区别。`flutter run` 只是对 `xcodebuild` / `gradle` 的包装，外加把 Dart 产物塞进构建流程。

**2. Dart 代码以什么形式进到 iOS 包里？**

- Debug 模式：Dart 代码由 JIT 运行（所以才有热重载）；
- Release 模式：Dart 被 AOT 编译成 arm64 机器码，打成 **`App.framework`** 塞进 .app 包，旁边还有引擎本体 **`Flutter.framework`**。
- 把 Dart 产物接进 Xcode 构建的桥梁是 `ios/Flutter/Generated.xcconfig`（flutter 工具生成，记录 SDK 路径和构建参数），所以先跑过一次 `flutter run` / `flutter build`，Xcode 才能独立构建成功。

**3. FlutterEngine 与 FlutterViewController 的关系（后面每一课的地基）。**

- **FlutterEngine**：Dart 的运行时，负责跑 Dart 代码 + 渲染。
- **FlutterViewController**：一个普通的 `UIViewController` 子类，把引擎渲染的内容显示出来。App 的 `rootViewController` 就是它。
- 一句话心智模型：**引擎是播放器内核，FlutterViewController 是播放窗口**。L8/L9 的 add-to-app 玩的就是"自己管理引擎和窗口"。

**4. 四条关键线程（引擎的 task runner，L1 讲回调线程的伏笔）。**

| 线程（task runner） | 跑什么 | iOS 类比 |
|---|---|---|
| Platform 线程 | 原生代码、channel 的原生侧回调、所有平台 API 调用 | **就是 iOS 主线程** |
| UI 线程 | 你的全部 Dart 代码（root isolate）、build/layout/paint 产出图层树 | 类似一个专用的串行队列，但跑的是另一门语言的运行时 |
| Raster 线程 | 把图层树栅格化上屏（Skia / Impeller） | 类似 Core Animation 的 render server 干的活 |
| IO 线程 | 图片解码、纹理上传等给 raster 打下手的活 | 你自己开的那条 decode / 预加载队列 |

面试常问"Flutter 有几条线程"——标准答案是**引擎给每个 engine 分配 4 个 task runner**（platform / UI / raster / IO）；其中只有 platform runner 用宿主主线程，其余是引擎自己的线程。

关键认知：**Dart 代码不在 iOS 主线程上跑**。所以 channel 通信本质是跨线程消息传递——这就是 L1 的入口。

**5. 新模板的两个新鲜细节（和老教程不一样的地方）。**

- iOS 侧已经有 `SceneDelegate.swift`，接入了 UIScene 生命周期（和你现在的原生工程一致）。
- `AppDelegate` 除了继承 `FlutterAppDelegate`，还实现了 `FlutterImplicitEngineDelegate`——插件注册挪到了 `didInitializeImplicitFlutterEngine` 回调里（老模板是在 `didFinishLaunching` 里直接 `GeneratedPluginRegistrant.register(with: self)`）。"隐式引擎"指的是模板帮你自动创建的那台引擎，与之相对的就是 L8/L9 里我们自己显式创建的引擎。

## 二、控件 / API 速查表

### Flutter 侧（本课新出现）

| 控件/API | iOS 类比 | 用法 & 关键参数 | 易踩的坑 |
|---|---|---|---|
| `MaterialApp` | `UIApplication` + `UIWindow` + 全局主题的合体 | `title`、`theme: ThemeData(colorSchemeSeed:)`、`home` | `title` 不是界面标题，是任务切换器里的名字；界面标题在 `AppBar` |
| `Scaffold` | 带导航栏的 VC 模板（`UIViewController` + `UINavigationBar` 布局骨架） | `appBar`、`body`、`floatingActionButton` | 没有 Scaffold 的页面弹 SnackBar 会崩；每个"页面"基本都该有一个 |
| `ListView.separated` | `UITableView` + 自动分隔线 | `itemCount`、`itemBuilder`、`separatorBuilder` | 懒加载：屏幕外的 item **不会 build**（写测试时找不到屏幕外的文本就是这个原因） |
| `ListTile` | `UITableViewCell` 的 `.subtitle` 样式 | `leading`/`title`/`subtitle`/`trailing`/`onTap` | 想要"禁用但可点提示"不能用 `enabled: false`（它会吞掉 onTap） |
| `CircleAvatar` | 圆形头像 `UIImageView`（layer.cornerRadius 那套的封装） | `child` 或 `backgroundImage` | 放文字时字号跟随主题，不跟 radius 自动缩放 |
| `SnackBar` + `ScaffoldMessenger` | 底部 toast/HUD；Messenger 类比全局单例管理器 | `ScaffoldMessenger.of(context).showSnackBar(...)` | 连点会排队，先 `hideCurrentSnackBar()` 再 show 体验更好 |
| `Navigator` + `MaterialPageRoute` | `UINavigationController.pushViewController` | `Navigator.of(context).push(MaterialPageRoute(builder:))` | Flutter 页面没有 VC 这层，`builder` 返回的就是页面 Widget 本身 |
| `WidgetBuilder` | `(BuildContext) -> Widget` 的 typedef，类比 `() -> UIViewController` 闭包 | 存起来延迟构建页面 | 它是函数不是 Widget，registry 里存它才能"用时才建页面" |
| switch 表达式 | Swift 的 `switch` 表达式（5.9 的 if/switch expression） | `trailing: switch (status) { A => x, B => y }` | 必须穷举；这是 Dart 3 语法，老教程里看不到 |

### 原生侧（本课新出现的概念）

| 概念/文件 | iOS 类比 | 说明 | 易踩的坑 |
|---|---|---|---|
| `Runner.xcworkspace` | 就是你熟悉的 workspace | 含 CocoaPods 时的打开入口 | 打开 `.xcodeproj` 会缺 Pods 依赖，编不过 |
| `FlutterAppDelegate` | `UIApplicationDelegate` 的 Flutter 版基类 | 帮你处理引擎、插件生命周期分发 | 自己的 AppDelegate 逻辑记得调 `super` |
| `FlutterViewController` | 普通 `UIViewController` 子类 | 引擎内容的"播放窗口" | 它不画 UIKit 控件，里面是引擎自绘的一整块 surface |
| `GeneratedPluginRegistrant` | 没有直接对应（想象 CocoaPods 自动生成的注册表） | flutter 工具生成，把 pubspec 里所有插件的原生侧注册进引擎 | 手动删改会被下次构建覆盖 |
| `Generated.xcconfig` | 就是 xcconfig | 把 Flutter SDK 路径/构建模式接进 Xcode 构建 | 没跑过一次 flutter 命令前它不存在，纯 Xcode 构建会失败 |
| `FlutterActivity` (Android) | 对应 iOS 侧 FlutterViewController + 宿主 | `MainActivity : FlutterActivity()` 一行就够 | Android 的"页面"是 Activity，没有 VC 概念 |
| `AndroidManifest.xml` | `Info.plist` | 权限声明、显示名、入口 Activity | 权限要双端各声明一次（L4 会真踩到） |

## 三、代码地图

```
native_lab/app/lib/
├── main.dart                      # NativeLabApp：MaterialApp + 主题，App 壳
├── home/lesson_list_page.dart     # 首页课程列表 + 门禁锁（locked 只弹提示）
└── lessons/
    ├── lesson.dart                # Lesson 模型 + LessonStatus 枚举
    ├── lesson_registry.dart       # 10 课注册表（进度表的可视化副本）
    └── l0/l0_anatomy_page.dart    # L0 入口页：原生工程解剖速查

native_lab/app/test/
└── lesson_list_test.dart          # 3 个 widget test：列表渲染/门禁提示/L0 跳转
```

## 四、双端对照

| 角色 | iOS | Android |
|---|---|---|
| App 入口 | `AppDelegate.swift`（+ `SceneDelegate.swift`） | `AndroidManifest.xml` 声明的入口 Activity |
| Flutter 宿主 | `FlutterViewController`（模板里由 storyboard 装配） | `MainActivity : FlutterActivity()` |
| 原生配置 | `Info.plist` | `AndroidManifest.xml` |
| 构建接线 | `Flutter/Generated.xcconfig` | `android/settings.gradle.kts` 里 include flutter 的 gradle 插件 |
| 依赖管理 | CocoaPods（`Podfile`，装了插件后出现） | Gradle（`build.gradle.kts`） |

## 五、自测清单

> 参考答案见 [自测答案/L0-自测答案.md](自测答案/L0-自测答案.md)（先自己答，再对照）。

1. `Runner.xcworkspace` 和 `Runner.xcodeproj`，该用 Xcode 打开哪个？为什么？
2. Release 包里，你写的 Dart 代码最终以什么形式存在于 .app 里？
3. Platform 线程和 UI 线程各自跑什么？哪条是 iOS 主线程？
4. 为什么改 App 显示名只动了 `Info.plist` 和 `AndroidManifest.xml`，一行 Dart 都不用改？

## 六、课后练习

不用 `flutter run`，直接用 Xcode 打开 `native_lab/app/ios/Runner.xcworkspace`，选一台模拟器点 Run，观察 App 一样能启动——体会"flutter CLI 只是包装"。（提示：本课已经跑过 `flutter build ios --simulator`，`Generated.xcconfig` 已就位，Xcode 能独立构建。）

## 七、面试高频题（附答案）

> 面试语境：这一课的内容基本是**开场热身题**——面试官想先确认"你知不知道 Flutter 在原生工程里到底是个什么东西"。答不好，后面深水区的题他就不问了。

**Q1. Flutter 的 Debug / Profile / Release 三种模式有什么区别？你的 Dart 代码在 Release 包里以什么形式存在？**

| 模式 | Dart 执行方式 | 特点 | 用途 |
|---|---|---|---|
| Debug | JIT（解释 + 即时编译） | 有热重载、断言开启、体积大、性能差 | 开发 |
| Profile | AOT（保留部分观测能力） | 无热重载，保留 timeline/DevTools 性能追踪 | 性能调优（必须真机） |
| Release | AOT（机器码） | 无调试信息、体积最小、性能最好 | 上线 |

产物落点：**iOS** 是 `App.framework`（你的 Dart AOT 产物）+ `Flutter.framework`（引擎）；**Android** 是 `libapp.so`（Dart AOT）+ `libflutter.so`（引擎）。Debug 模式下 iOS 的 `App.framework` 里装的是 kernel snapshot（`.dill`），由 JIT 跑。

一句话记法：**Debug 是解释执行才有热重载，Release 是编译成 arm64 机器码所以没有。**

**Q2. Flutter 有几条线程？Dart 代码跑在哪条？为什么这决定了 channel 必须是异步的？**

四条 task runner：Platform（= iOS 主线程）、UI（跑 root isolate 里的全部 Dart 代码）、Raster（栅格化上屏）、IO（图片解码/纹理上传）。

你的 Dart 代码在 **UI 线程**，原生 API 只能在 **Platform 线程**调用——两者是不同线程，跨线程只能靠消息投递，**投递就意味着不能原地拿到返回值**。所以 `invokeMethod` 返回 `Future`，原生 handler 回 `result` 也是异步。想要"同步调原生"只有绕开 channel 走 `dart:ffi`。

**Q3. FlutterEngine 和 FlutterViewController 是什么关系？一个 App 能有几个引擎？**

- `FlutterEngine` = Dart 运行时 + 渲染管线，**真正干活的内核**；它自己能在没有任何 UI 的情况下 `run()`（L9 的预热就是这么干的）。
- `FlutterViewController` = 一个普通的 `UIViewController` 子类，**只是引擎渲染内容的展示窗口**。
- 一个 App 可以有多个引擎；引擎与 VC 是一对一显示、但引擎可以脱离 VC 存活（页面关了引擎还在 → L9 的"状态常驻"）。多个并存的 Flutter 界面应该用 `FlutterEngineGroup`（L9）。

**Q4. Flutter 的 UI 是用原生控件画的吗？和 React Native 有什么本质区别？**

不是。Flutter **自绘**：引擎拿到一块画布（iOS 上是一个 `UIView` 的 layer），所有按钮、文字、列表都由 Skia / Impeller 自己画出来，**不映射成 `UIButton`/`UILabel`**。RN 是把 JS 描述翻译成真实原生控件树。

带来的三个结论：① 双端渲染一致性好、不受系统控件版本差异影响；② 无法用原生控件调试工具（Xcode View Hierarchy 里只看到一整块 surface）；③ 想用真实原生控件（地图/WebView）就必须走 **PlatformView**（L6）。

补充：iOS 端 Flutter 3.10 起默认渲染后端从 Skia 换成了 **Impeller**（预编译 shader，消除首帧卡顿），Android 侧也逐步默认。

**Q5. 接入 Flutter 后 App 包会大多少？为什么？**

Release 下大致增量在 **几 MB 到十几 MB**（随架构和资源变化，iOS arm64 通常 ~10MB 起）：引擎本体（`Flutter.framework`/`libflutter.so`）占大头，其次是 ICU 数据、你的 Dart AOT 产物和字体/图标资源。

减包常见手段：只保留必要 ABI（Android 用 `--split-per-abi` 或 app bundle）、去掉未用字体（`--tree-shake-icons` 默认开）、图片资源走网络/按需下载、`--split-debug-info` + `--obfuscate` 剥离符号。

**Q6. `flutter run` 到底做了什么？没跑过 flutter 命令，为什么 Xcode 直接编译会失败？**

`flutter run` ≈ ① 编译 Dart（Debug 出 kernel、Release 走 AOT）→ ② 生成 `ios/Flutter/Generated.xcconfig` 等接线文件 → ③ 调 `xcodebuild` / `gradle` 构建原生工程 → ④ 装机、启动、attach 上调试器（热重载通道）。

`Generated.xcconfig` 记录了 Flutter SDK 路径、构建模式、产物目录，是 Xcode 构建里 `Run Script` 阶段的输入。**没生成它，Xcode 独立构建就找不到 Flutter 产物而失败**——这也是很多"接手别人工程编不过"的第一个坑：先跑一次 `flutter pub get` + `flutter build`。

**Q7. 热重载（hot reload）的原理是什么？哪些改动它救不了？**

原理：Debug 的 JIT 模式下，工具把**改动的 Dart 代码增量编译成 kernel 片段推给运行中的 VM**，替换掉旧的函数实现，然后触发一次 `reassemble` 重建 widget 树——**state 保留**（所以页面停在原处）。

救不了的情形（要 hot restart 或重编）：
- `main()`、`initState` 里已经跑过的一次性逻辑不会重跑；
- 全局变量 / `static` 字段的初始值已求值，不会更新；
- 结构性改动：改类的父类、把 class 改成 enum、改泛型签名；
- **原生代码（Swift/Kotlin）改动**——那要重新构建整个 App，热重载完全无关。

一句话记法：**热重载换的是"函数体"，换不了"结构"和"已经发生过的事"，更换不了原生。**
