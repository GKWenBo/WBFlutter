# L2 数据编解码与复杂参数

> 企业场景：埋点桥——Flutter 的埋点统一走原生已接好的统计 SDK（友盟/GA/自研）。事件名 + 一坨属性从 Dart 灌到原生，原生回执，还能把缓存的事件批量拉回来。这一课把 MethodChannel 底下的"搬运工" **StandardMessageCodec** 彻底讲透。

## 一、本课要掌握什么

**1. StandardMessageCodec 是谁、干什么。**

L1 你只传了方法名。这课要传**结构化数据**：`invokeMethod('logEvent', {嵌套 Map})`。这个 Map 怎么变成二进制、跨过线程、再在 Swift 侧变回 `NSDictionary`？靠的就是 MethodChannel 默认挂的编解码器 **StandardMessageCodec**。

心智模型：**codec = 一张固定的类型对照表 + 一套二进制打包规则**。它只认表上的类型（null/bool/int/double/String/List/Map/几种 TypedData）。你塞一个它不认识的东西（比如自定义类实例、`DateTime`、`Uint16List`）进去，**编码那一刻就抛**，根本过不了桥。所以 L1 我们才强调"过桥前先 `toMap()` 拍平"——自定义类必须先降解成 codec 认识的裸结构。

iOS 类比：像 `NSKeyedArchiver`/`Codable` 之于对象持久化——只有能被序列化的类型才能过关，中间是二进制而不是 JSON 文本（**比 JSON 快、且能零拷贝传字节**，见下文二进制）。

**2. ★ 类型映射表（本课核心，必须背下来）。**

| Dart | Swift (iOS) | Kotlin (Android) | 说明 |
|---|---|---|---|
| `null` | `NSNull` / `nil` | `null` | |
| `bool` | `NSNumber` → `Bool` | `Boolean` | Swift 侧数字都裹在 NSNumber 里 |
| `int`（≤32 位） | `NSNumber` → `Int` | `Integer` (`Int`) | |
| `int`（>32 位） | `NSNumber` → `Int` | **`Long`** | ⚠️ Android 大整数是 Long，见坑 2 |
| `double` | `NSNumber` → `Double` | `Double` | ⚠️ 与 int 不可混，见坑 1 |
| `String` | `String` (`NSString`) | `String` | |
| `Uint8List` | `FlutterStandardTypedData` | `ByteArray` | 二进制零拷贝 |
| `Int32List`/`Int64List`/`Float64List` | `FlutterStandardTypedData` | `IntArray`/`LongArray`/`DoubleArray` | 数值数组高效传 |
| `List<Object?>` | `NSArray` / `[Any]` | `List<Any?>` | 元素递归编码 |
| `Map<Object?,Object?>` | `NSDictionary` / `[AnyHashable: Any]` | `Map<Any?,Any?>` | 键值都是 `Object?` |

**记忆钩子**：Swift 侧数字**全是 `NSNumber`**（取值 `.intValue`/`.doubleValue`/`.boolValue`），所以 iOS 几乎没有数字类型坑；Kotlin 侧数字**按大小/精度分裂成 `Integer`/`Long`/`Double`**，坑都在这。回 Dart 时，容器里的键值静态类型都是 `Object?`，**必须 `.cast` 或 `invokeMapMethod`/`invokeListMethod` 收窄**才好用。

**3. 三大类型事故（本课暗线，延续 L1 的失败路径教学）。**

- **坑 1 · int/double 不可混。** codec **不做隐式数字转换**。原生约定回 `int` 却回了 `double`（比如把 `3` 写成 `3.0`，或返回 `NSTimeInterval`），Dart 侧 `invokeMethod<int>` 在 `as int` 时抛 **`TypeError`**——崩在 **Dart 端**。这就是 L1 `getSystemUptime` 为什么必须用 `double` 接（`ProcessInfo.systemUptime` 是 `Double`）。本课测试 `类型坑：原生返回 double 却用 <int> 接` 亲手复现了它。
- **坑 2 · Android 的 Int/Long 边界。** Dart `int` 是 64 位；codec 编码时按值大小选 int32 或 int64。到 Kotlin 侧，小值是 `Integer`、大值（超 int32）是 `Long`。所以 `map["ts"] as Int` 对一个毫秒时间戳（大数）会 **`ClassCastException`**，稳妥写法 `(map["ts"] as Number).toLong()`。iOS 侧 `NSNumber` 统一，无此坑。
- **坑 3 · `Map<Object?,Object?>` 直接当 `Map<String,dynamic>` 用会崩。** 原生传回的容器键值都是 `Object?`，`as Map<String, Object?>` 是失败的强转。要用 `.cast<String, Object?>()`（运行时逐个检查）或顶层用 `invokeMapMethod`/`invokeListMethod`。本课 `AnalyticsEvent.fromMap` 和 `fetchLoggedEvents` 都在做这个收窄。

**4. 二进制为什么用 `Uint8List` 而不是 base64 String。**

传字节流（缩略图、protobuf、加密包）时，`Uint8List` 走 codec 的 **typed-data 分支**：直接按字节打包，**零拷贝、无膨胀**。若退化成 base64 `String`：体积涨 ~33%、两端各多一次编解码 CPU。所以规矩是——**二进制就用 `Uint8List`/`ByteArray`，别 base64**。

**5. ★ 补齐全家福：三种 channel + 四种 codec（本课的知识地图，面试爱问）。**

前面你只见过 `MethodChannel`。其实 Flutter 有**三种 channel**，它们**共用同一个传输层**（`BinaryMessenger`），区别只在"调用语义 + 用哪种 codec"：

| Channel | 语义 | 默认 codec | 典型用途 |
|---|---|---|---|
| `MethodChannel` | 一问一答的 RPC（带方法名） | `StandardMethodCodec`（内部套 `StandardMessageCodec`） | L1/L2/L4：调一个原生能力 |
| `EventChannel` | 订阅-推送的流 | `StandardMethodCodec` | L3：持续监听原生状态 |
| `BasicMessageChannel` | **没有方法名**的裸消息，双向对等，两边都能主动发、都能回一条 | 可选（见下表） | 高频小消息、双向对话式协议；**Pigeon 生成的通道用的就是它**（L5 会印证） |

**四种 codec**（`BasicMessageChannel` 可自由挑）：

| Codec | 支持类型 | 说明 |
|---|---|---|
| `StandardMessageCodec` | 上面那张类型映射表的全部 | 默认；二进制紧凑，支持 TypedData 零拷贝 |
| `JSONMessageCodec` | JSON 能表达的（无二进制、无非 String 键） | 和现成的 JSON 协议对接方便，但**更慢、更胖** |
| `StringCodec` | 只有 `String`（UTF-8） | 纯文本协议 |
| `BinaryCodec` | 只有 `ByteData`（原样透传） | **完全不编解码**，自己定二进制格式（protobuf/自研）时用 |

一句话记法：**MethodChannel 是"带方法名的 BasicMessageChannel"，EventChannel 是"约定了 listen/cancel 两个方法名的 MethodChannel"**——三者是同一根管子上的三种协议约定。

> 顺带回答一个常见疑问：**MethodChannel 底层怎么表示一次调用？** `StandardMethodCodec` 把调用编成 `[方法名, 参数]`，把成功回执编成 `[返回值]`（一个元素的 List），把错误回执编成 `[code, message, details]`（三个元素）。L5 里我们用 `setMockDecodedMessageHandler` 顶替 Pigeon 的私有通道时，之所以要回 `[返回值]` 这种 List，就是这个规则在起作用。

## 二、控件 / API 速查表

### Dart 侧

| API | iOS 类比 | 用法 & 关键点 | 坑 |
|---|---|---|---|
| `invokeMethod(method, arguments)` | 带参 RPC | 第二参 = 传给原生的数据，任意 codec 支持的嵌套结构 | 塞不支持类型→编码期抛 |
| `invokeMapMethod<K,V>()` | 返回收窄成 Map | 顶层是 Map 时用，省手动 cast | 仍可能返回 null |
| `invokeListMethod<T>()` | 返回收窄成 List | 顶层是 List 时用 | 元素仍是 `Object?`，要再 cast |
| `.cast<K,V>()` | 类型收窄 | 把 `Map<Object?,Object?>` 逐项收成 `Map<String,Object?>` | 与 `as` 不同：cast 是运行时逐个检查 |
| `Uint8List` | `NSData` | 二进制载荷 | 只有它零拷贝，别用 base64 |

### Swift 侧

| API | 用法 & 关键点 |
|---|---|
| `call.arguments as? [String: Any]` | Dart Map → NSDictionary；数字取 `.intValue`/`.doubleValue` |
| `call.arguments as? FlutterStandardTypedData` | Dart `Uint8List` 的原生落点，`.data` 是 `Data` |
| `result(buffer)` | `[[String:Any]]` 自动编码回 Dart 的 `List<Map>` |

### Kotlin 侧

| API | 用法 & 关键点 |
|---|---|
| `call.arguments as? Map<String, Any?>` | 数字用 `(x as Number).toLong()/.toDouble()`，别裸 `as Int` |
| `call.arguments as? ByteArray` | Dart `Uint8List` 的原生落点，直接 `.size` |

## 三、代码地图

```
native_lab/app/lib/lessons/l2/
├── analytics_event.dart      # AnalyticsEvent 模型：name + properties(Map)，toMap/fromMap 收口
├── analytics_bridge.dart     # AnalyticsBridge：第二条 channel，logEvent/fetchLoggedEvents/uploadRawLog
└── l2_analytics_page.dart    # 演示台：上报/拉取/传二进制 三按钮

native_lab/app/ios/Runner/AppDelegate.swift
    └── enum AnalyticsBridge   # 原生侧 handler，内存 buffer 模拟统计 SDK（与 DeviceInfoBridge 并列注册）

native_lab/app/android/app/src/main/kotlin/.../MainActivity.kt
    └── configureFlutterEngine # 第二条 channel 并列追加（analyticsBuffer 模拟 SDK）

native_lab/app/test/l2_analytics_test.dart  # 4 桥单测（嵌套/列表/二进制/类型坑）+ 2 页面 widget test
```

## 四、双端对照

| 对照点 | iOS (Swift) | Android (Kotlin) |
|---|---|---|
| 数字桥接 | 全是 `NSNumber`，`.intValue`/`.doubleValue` 取值，无大小坑 | 分裂 `Integer`/`Long`/`Double`，大整数 `as Int` 会崩，用 `(x as Number).toLong()` |
| 二进制落点 | `FlutterStandardTypedData`，`.data`(`Data`) | 直接 `ByteArray`，`.size` |
| Map 类型 | `[String: Any]`（NSDictionary） | `Map<String, Any?>`（HashMap） |
| 第二条 channel | `AnalyticsBridge.register(...)` 与 DeviceInfoBridge 并列 | configureFlutterEngine 里并列再 new 一个 MethodChannel |

## 五、自测清单

> 参考答案见 [自测答案/L2-自测答案.md](自测答案/L2-自测答案.md)（做完再对照）。

1. Dart 的 `int` 传到 Swift、Kotlin 各变成什么类型？为什么 Android 侧 `map["ts"] as Int` 有时会崩、正确写法是什么？
2. 原生约定回 `int` 却回了 `double`，Dart 用 `invokeMethod<int>` 接会发生什么？崩在**哪一端**、什么异常？这跟 L1 的 `getSystemUptime` 用 `double` 有什么关系？
3. 要把一张缩略图的字节传给原生，用什么类型？换成 base64 `String` 传有什么代价？
4. `fetchLoggedEvents()` 里 `invokeListMethod` 返回的 List，每个元素的静态类型是什么？为什么还要对每个元素 `.cast<String,Object?>()`？

## 六、课后练习

给埋点桥加**批量上报** `logBatch(List<AnalyticsEvent>)`（一次把多条事件灌下去，减少过桥次数——真实 SDK 常这么做）：
- Dart：`AnalyticsBridge.logBatch(List<AnalyticsEvent> events) -> Future<int>`，`invokeMethod('logBatch', events.map((e) => e.toMap()).toList())`，返回 buffer 新总数；
- Swift：`case "logBatch": guard let list = call.arguments as? [[String: Any]] ...; buffer.append(contentsOf: list); result(buffer.count)`；
- Kotlin：`"logBatch" -> { val list = call.arguments as? List<Map<String, Any?>>; ... analyticsBuffer.addAll(list); result.success(analyticsBuffer.size) }`；
- 补一条 mock 单测：传 3 条事件，mock 原生返回 3，断言 `logBatch()` 得到 3，并校验原生收到的 `call.arguments` 是长度为 3 的 List。

（提示：这题练的是"List 作为顶层参数"——注意三端把 `List<Map>` 转成对应容器类型时的写法差异。）

> ✅ **本练习已在仓库落地为参考实现**（本轮由讲师完成）：Dart [analytics_bridge.dart](../../app/lib/lessons/l2/analytics_bridge.dart) `logBatch`、Swift/Kotlin 各 `case "logBatch"`、单测 `课后练习·批量上报`。三端要点对照：
> - **顶层是 List 而非 Map**：`invokeMethod('logBatch', events.map((e) => e.toMap()).toList())`；codec 顶层直接支持 List。
> - **Swift**：`call.arguments as? [[String: Any]]`（NSArray→Swift 数组），`buffer.append(contentsOf:)`。
> - **Kotlin**：`call.arguments as? List<Map<String, Any?>>`（需 `@Suppress("UNCHECKED_CAST")`），`analyticsBuffer.addAll(list)`。
> - 三端都回 `buffer.count/size`（累加后的新总数），Dart 侧 `invokeMethod<int>` 接。

## 七、面试高频题（附答案）

> 面试语境：这一课的题往往以"你踩过什么坑"的形式出现——**能说出 int/double 和 Android Long 这两个具体事故，比背概念有说服力得多**。

**Q1. Flutter 和原生之间能传哪些类型？自定义对象怎么过桥？**

只能传 `StandardMessageCodec` 认识的类型：`null`/`bool`/`int`/`double`/`String`/`Uint8List` 等 TypedData/`List`/`Map`。**自定义类不能直接传**——塞进去在**编码那一刻就抛**，根本到不了原生。

三种做法：
1. **拍平成 Map**（本课做法）：`toMap()` / `fromMap()`，在**桥接层**完成，别让裸 Map 流进业务代码；
2. **扩展 codec**：继承 `StandardMessageCodec` 覆写 `writeValue`/`readValueOfType`，用自定义 type id（**≥128，低位是 Flutter 保留的**）编自己的类——三端都要写一份，容易漂移；
3. **用 Pigeon**（L5）：契约里声明数据类，生成器**自动**替你干第 2 件事，还带编译期检查。**这是团队协作下的正解。**

**Q2. Dart 的 `int` 传到 Android 侧是什么类型？为什么 `as Int` 会崩？**

Dart 的 `int` 是 64 位。codec 编码时**按值大小**选 int32 或 int64，所以到 Kotlin 侧：小值是 `Integer`，超过 int32 范围的（**毫秒时间戳、雪花 ID、文件字节数**都超）是 `Long`。裸写 `map["ts"] as Int` 在大值上必然 `ClassCastException`。

稳妥写法：`(map["ts"] as Number).toLong()`。iOS 侧无此坑——所有数字都裹成 `NSNumber`，取 `.intValue`/`.doubleValue` 即可。

**面试加分**：指出这是个**只在部分数据下才崩的坑**（小 id 测试全过，线上真实时间戳一来就崩），所以更要在写代码时就用 `Number` 兜住，而不是等测试发现。

**Q3. 原生约定返回 `int` 却返回了 `double`，会发生什么？崩在哪一端？**

codec **不做隐式数字转换**。Dart 侧 `invokeMethod<int>` 拿到的实际是 `double`，在 `as int` 时抛 **`TypeError`**，**崩在 Dart 端**——但根因在原生端。

最常见的触发源：原生返回的是 `NSTimeInterval`/`Float`/除法结果（`3` 写成 `3.0`）。防御写法：Dart 侧用 `num` 接再 `.toInt()`，或者干脆上 Pigeon 让契约钉死类型。

**Q4. 为什么用二进制 codec 而不是直接传 JSON 字符串？**

三点：① **更快**——二进制无需文本解析，数字直接按定长写；② **更小**——没有字段名冗余以外的文本膨胀，且 `Uint8List` 走 typed-data 分支**零拷贝**；③ **类型更全**——JSON 没有二进制类型、没有非 String 的 Map 键、int/double 也分不清。

`JSONMessageCodec` 确实存在，适合"原生侧已有一套 JSON 协议要直接对接"的场景，但默认不该选它。

**Q5. 要把一张图片 / 一段视频帧传给原生，怎么传？**

- 小的字节流（缩略图、加密包、protobuf）：`Uint8List` ↔ `FlutterStandardTypedData`(iOS) / `ByteArray`(Android)，**零拷贝**；
- **千万别 base64 成 String**：体积涨 ~33%，两端各多一次编解码；
- **大文件/视频**：根本不要过桥——传**文件路径**或 `content://` URI，让原生自己去读；
- **相机预览/视频帧这种每帧数据**：走 **Texture（外接纹理）** 或 PlatformView，把像素留在 GPU 侧，channel 只传控制指令。

一句话记法：**桥上传"指令和小数据"，大数据传"地址"，像素传"纹理"。**

**Q6. 从原生拿回来的 `Map`，为什么不能直接 `as Map<String, dynamic>`？**

因为 codec 解出来的静态类型是 **`Map<Object?, Object?>`**，`as Map<String, Object?>` 是一次**失败的强转**（运行时类型不匹配直接抛）。

两种正解：顶层用 `invokeMapMethod<String, Object?>()` / `invokeListMethod<T>()` 让框架帮你收窄；嵌套层用 `.cast<String, Object?>()`（运行时逐项检查）。注意 `.cast` 是**惰性检查**，元素取出时才校验类型——所以更推荐直接 `Map<String, Object?>.from(raw)` 一次性拷贝收口。

**Q7. 三种 channel 分别是什么？`BasicMessageChannel` 什么时候用？**

见上文全家福表。`BasicMessageChannel` 的特点是**没有方法名、双向对等**：两端都能主动 `send`，对方回一条即可。适合：

- **高频小消息**（省掉方法名字符串的编解码）；
- **双向对话式协议**（不是"我调你一个函数"，而是"我们互相通报状态"）；
- **自定义 codec**（配 `BinaryCodec` 自己定二进制格式，比如直接跑 protobuf）。

冷知识加分点：**Pigeon 生成的通道底层用的就是 `BasicMessageChannel`**，所以 L5 里测试要在这条私有通道上 mock。
