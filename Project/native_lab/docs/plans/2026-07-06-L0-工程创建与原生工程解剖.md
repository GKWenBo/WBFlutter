# NativeLab L0：工程创建与原生工程解剖 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **教学项目特别说明**：本计划的"执行"是一堂课——每个任务边写代码边给学员讲解（iOS 类比先行），验证环节要截模拟器图给学员看。L1–L9 各自在开课时另写计划，本计划只覆盖 L0。

**Goal:** 建好 NativeLab 主 App 骨架：课程列表首页（10 课注册表 + 门禁锁）、L0 原生工程解剖入口页、进度索引与 L0 课时文档，全部通过 analyze/test 并在 iPhone 模拟器跑通。

**Architecture:** 单 Flutter 工程 `native_lab/app/`，无状态管理库（setState 都用不上，纯静态注册表），Navigator.push 导航。课程注册表硬编码在 Dart 里，进度真身在 `docs/lessons/README.md`。

**Tech Stack:** Flutter stable（与 wan_android 同一 SDK）、Material 3；测试用 flutter_test 自带 widget test。

## Global Constraints（来自设计文档，每个任务默认遵守）

- 设计文档唯一权威：`native_lab/docs/2026-07-06-原生交互教学课程设计.md`。
- 目录 `native_lab/app/`，Dart 包名 `native_lab`，App 显示名「NativeLab 原生实验室」。
- 代码注释：详细中文 + 每个新概念标注"对应 iOS 的什么 + 差异"。
- 质量门：`flutter analyze` 0 issue；**全量** `flutter test` 通过（不只跑新文件）。
- git：只 `git add native_lab/`；中文提交信息；**每课一次提交，且提交前必须等学员口头确认过关**（所以只有最后一个任务有 commit 步骤）。
- 验证设备：iPhone 模拟器，截图命令 `xcrun simctl io booted screenshot <路径>`。

---

### Task 1: 创建 Flutter 工程 + 模拟器冒烟

**Files:**
- Create: `native_lab/app/`（flutter create 生成整个工程）
- Modify: `native_lab/app/ios/Runner/Info.plist`（App 显示名）
- Modify: `native_lab/app/android/app/src/main/AndroidManifest.xml`（App 显示名）

**Interfaces:**
- Consumes: 无（起点任务）
- Produces: 可运行的 Flutter 工程骨架，Task 2 在 `lib/` 下重写页面

- [ ] **Step 1: 创建工程（只要 ios/android 两个平台）**

```bash
cd /Users/wenbo/Desktop/WBFlutter
flutter create --project-name native_lab --org com.wenbo --platforms ios,android native_lab/app
```

Expected: `All done!`，生成 `native_lab/app/{lib,ios,android,test,pubspec.yaml}`。

教学点（创建完就带学员看）：`ios/` 是一个**完整的 Xcode 工程**（这是 L0 的核心认知），`android/` 是完整 Gradle 工程；`flutter create` 的产物没有魔法。

- [ ] **Step 2: 改 App 显示名**

`ios/Runner/Info.plist` 里把 `CFBundleDisplayName` 的值改为：

```xml
<key>CFBundleDisplayName</key>
<string>NativeLab 原生实验室</string>
```

`android/app/src/main/AndroidManifest.xml` 里 application 标签的 label 改为：

```xml
android:label="NativeLab 原生实验室"
```

教学点：这就是双端各自的"原生配置"，Flutter 不接管它们——iOS 同学熟悉的 Info.plist 原封不动在这。

- [ ] **Step 3: 质量门冒烟**

```bash
cd native_lab/app && flutter analyze && flutter test
```

Expected: `No issues found!` + `All tests passed!`（此时还是官方 counter demo 和它的默认测试）。

- [ ] **Step 4: 模拟器跑通 + 截图**

```bash
open -a Simulator
cd native_lab/app && flutter run
# 另一终端截图：
xcrun simctl io booted screenshot /tmp/l0_scaffold.png
```

Expected: counter demo 正常显示，桌面图标名为「NativeLab 原生实验室」。截图给学员确认。

---

### Task 2: 课程注册表 + 课程列表首页（TDD）

**Files:**
- Create: `native_lab/app/lib/lessons/lesson.dart`
- Create: `native_lab/app/lib/lessons/lesson_registry.dart`
- Create: `native_lab/app/lib/lessons/l0/l0_anatomy_page.dart`
- Create: `native_lab/app/lib/home/lesson_list_page.dart`
- Modify: `native_lab/app/lib/main.dart`（整个重写）
- Create: `native_lab/app/test/lesson_list_test.dart`
- Delete: `native_lab/app/test/widget_test.dart`（counter 测试随 demo 一起删）

**Interfaces:**
- Consumes: Task 1 的工程骨架
- Produces: `enum LessonStatus { locked, inProgress, done }`；`class Lesson`（字段 `String id, String title, String scenario, LessonStatus status, WidgetBuilder? pageBuilder`）；`final List<Lesson> lessonRegistry`（10 项）；`class NativeLabApp`、`class LessonListPage`、`class L0AnatomyPage`。后续每课开课时改 registry：状态翻牌 + 填 pageBuilder。

- [ ] **Step 1: 先写失败的测试**

`test/lesson_list_test.dart`：

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:native_lab/lessons/lesson_registry.dart';
import 'package:native_lab/main.dart';

void main() {
  testWidgets('课程列表展示 10 个课时', (tester) async {
    await tester.pumpWidget(const NativeLabApp());
    expect(lessonRegistry.length, 10);
    expect(find.text('工程创建与原生工程解剖'), findsOneWidget);
    expect(find.text('MethodChannel：Flutter 调原生'), findsOneWidget);
  });

  testWidgets('点击锁定课时只弹提示，不跳转', (tester) async {
    await tester.pumpWidget(const NativeLabApp());
    await tester.tap(find.text('MethodChannel：Flutter 调原生'));
    await tester.pump(); // 让 SnackBar 开始入场
    expect(find.text('先完成前面的课时，再解锁 L1'), findsOneWidget);
  });

  testWidgets('点击 L0 进入原生工程解剖页', (tester) async {
    await tester.pumpWidget(const NativeLabApp());
    await tester.tap(find.text('工程创建与原生工程解剖'));
    await tester.pumpAndSettle(); // 等页面转场动画走完
    expect(find.text('L0 原生工程解剖'), findsOneWidget);
  });
}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd native_lab/app && flutter test test/lesson_list_test.dart
```

Expected: FAIL，报 `Target of URI doesn't exist`（lesson_registry.dart 还不存在）。

- [ ] **Step 3: 实现课程模型**

`lib/lessons/lesson.dart`：

```dart
import 'package:flutter/widgets.dart';

/// 课时状态。iOS 类比：就是 Swift 的 `enum LessonStatus { case locked... }`，
/// Dart 的 enum 也支持 switch 穷举检查。
enum LessonStatus {
  locked, // 未解锁：前置课时还没过关
  inProgress, // 进行中：当前正在学的课
  done, // 已完成：模拟器跑通 + analyze 0 + 全量测试过 + 学员确认
}

/// 一条课程注册信息。
/// 设计文档约定：注册表硬编码、不做持久化——进度的"真身"在
/// docs/lessons/README.md，这里只是它在 App 内的可视化副本。
class Lesson {
  const Lesson({
    required this.id,
    required this.title,
    required this.scenario,
    required this.status,
    this.pageBuilder,
  });

  final String id; // 'L0'...'L9'
  final String title; // 课程主题
  final String scenario; // 企业真实场景，列表里当副标题
  final LessonStatus status;

  /// 课程入口页构造器。iOS 类比：类似存一个 `() -> UIViewController` 闭包，
  /// 用的时候才真正创建页面。还没实现的课时为 null。
  final WidgetBuilder? pageBuilder;
}
```

`lib/lessons/lesson_registry.dart`（10 项照设计文档大纲抄，标题/场景一字不差）：

```dart
import 'lesson.dart';
import 'l0/l0_anatomy_page.dart';

/// 课程注册表：App 首页列表的数据源。
/// 每课过关提交时手动翻状态；下一课开课时把它的 status 改成
/// inProgress 并补上 pageBuilder。
final List<Lesson> lessonRegistry = [
  Lesson(
    id: 'L0',
    title: '工程创建与原生工程解剖',
    scenario: '入职第一天：看懂 ios/ 和 android/ 到底是什么',
    status: LessonStatus.inProgress,
    pageBuilder: (_) => const L0AnatomyPage(),
  ),
  const Lesson(
    id: 'L1',
    title: 'MethodChannel：Flutter 调原生',
    scenario: '设备信息上报（机型/系统版本/电池）',
    status: LessonStatus.locked,
  ),
  const Lesson(
    id: 'L2',
    title: '数据编解码与复杂参数',
    scenario: '埋点桥：Flutter 埋点走原生统计 SDK',
    status: LessonStatus.locked,
  ),
  const Lesson(
    id: 'L3',
    title: 'EventChannel：原生持续推流',
    scenario: '网络状态监听（弱网提示、断网重连）',
    status: LessonStatus.locked,
  ),
  const Lesson(
    id: 'L4',
    title: '页面级混合 + 权限',
    scenario: 'Flutter 页跳原生扫码页，结果回传',
    status: LessonStatus.locked,
  ),
  const Lesson(
    id: 'L5',
    title: 'Pigeon 类型安全生成',
    scenario: '手写 channel 魔法值重构为 Pigeon',
    status: LessonStatus.locked,
  ),
  const Lesson(
    id: 'L6',
    title: 'PlatformView：视图级混合',
    scenario: '页面里嵌原生地图 MKMapView',
    status: LessonStatus.locked,
  ),
  const Lesson(
    id: 'L7',
    title: '插件开发',
    scenario: '把设备信息桥抽成独立 plugin 包',
    status: LessonStatus.locked,
  ),
  const Lesson(
    id: 'L8',
    title: 'add-to-app：原生工程接入 Flutter',
    scenario: '已有原生 App 用 CocoaPods 接入 Flutter 模块',
    status: LessonStatus.locked,
  ),
  const Lesson(
    id: 'L9',
    title: 'add-to-app：引擎管理与通信',
    scenario: '引擎预热、原生↔模块双向通信、路由协调',
    status: LessonStatus.locked,
  ),
];
```

- [ ] **Step 4: 实现 L0 入口页（原生工程解剖速查）**

`lib/lessons/l0/l0_anatomy_page.dart`：

```dart
import 'package:flutter/material.dart';

/// L0 入口页：把"原生工程解剖"的结论做成页内速查。
/// 本课没有任何 channel 代码——先认识地形，L1 才开始通信。
class L0AnatomyPage extends StatelessWidget {
  const L0AnatomyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('L0 原生工程解剖')),
      body: ListView(
        children: const [
          _SectionHeader('ios/ —— 一个完整的 Xcode 工程'),
          _AnatomyTile(
            path: 'Runner.xcworkspace',
            role: '用 Xcode 打开的入口（含 CocoaPods 依赖），和你平时的 workspace 一回事',
          ),
          _AnatomyTile(
            path: 'Runner/AppDelegate.swift',
            role: 'App 入口，继承 FlutterAppDelegate；插件在这里完成注册',
          ),
          _AnatomyTile(
            path: 'Runner/Info.plist',
            role: '原封不动的 Info.plist，权限描述、显示名都在这改',
          ),
          _AnatomyTile(
            path: 'Flutter/Generated.xcconfig',
            role: 'flutter 工具生成的构建配置，把 Dart 产物接进 Xcode 构建',
          ),
          _SectionHeader('android/ —— 一个完整的 Gradle 工程'),
          _AnatomyTile(
            path: 'app/src/main/kotlin/.../MainActivity.kt',
            role: '继承 FlutterActivity，对应 iOS 侧的 FlutterViewController 宿主',
          ),
          _AnatomyTile(
            path: 'app/build.gradle.kts',
            role: '模块构建脚本，applicationId/签名/依赖，类比 project.pbxproj + Podfile',
          ),
          _AnatomyTile(
            path: 'app/src/main/AndroidManifest.xml',
            role: '类比 Info.plist：权限声明、App 显示名、入口 Activity',
          ),
          _SectionHeader('引擎与线程（L1 前必备概念）'),
          _AnatomyTile(
            path: 'FlutterEngine',
            role: 'Dart 代码的运行时。UI 线程跑 Dart，Platform 线程就是 iOS 主线程',
          ),
          _AnatomyTile(
            path: 'FlutterViewController',
            role: '把引擎渲染的内容装进 UIKit 的容器 VC，App 的 rootViewController 就是它',
          ),
        ],
      ),
    );
  }
}

class _SectionHeader extends StatelessWidget {
  const _SectionHeader(this.title);
  final String title;

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.fromLTRB(16, 24, 16, 8),
      child: Text(title, style: Theme.of(context).textTheme.titleMedium),
    );
  }
}

class _AnatomyTile extends StatelessWidget {
  const _AnatomyTile({required this.path, required this.role});
  final String path;
  final String role;

  @override
  Widget build(BuildContext context) {
    return ListTile(
      dense: true,
      leading: const Icon(Icons.description_outlined),
      title: Text(path),
      subtitle: Text(role),
    );
  }
}
```

- [ ] **Step 5: 实现课程列表首页 + 重写 main.dart**

`lib/home/lesson_list_page.dart`：

```dart
import 'package:flutter/material.dart';

import '../lessons/lesson.dart';
import '../lessons/lesson_registry.dart';

/// 首页：课程列表，就是进度表在 App 内的可视化副本。
class LessonListPage extends StatelessWidget {
  const LessonListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('NativeLab 原生实验室')),
      body: ListView.separated(
        itemCount: lessonRegistry.length,
        separatorBuilder: (_, _) => const Divider(height: 1),
        itemBuilder: (context, index) =>
            _LessonTile(lesson: lessonRegistry[index]),
      ),
    );
  }
}

class _LessonTile extends StatelessWidget {
  const _LessonTile({required this.lesson});
  final Lesson lesson;

  @override
  Widget build(BuildContext context) {
    return ListTile(
      leading: CircleAvatar(child: Text(lesson.id)),
      title: Text(lesson.title),
      subtitle: Text(lesson.scenario),
      // 状态图标用 switch 表达式穷举，漏一个状态编译器直接报错。
      // iOS 类比：Swift 的 switch 穷举 enum 同款好处。
      trailing: switch (lesson.status) {
        LessonStatus.locked => const Icon(Icons.lock_outline),
        LessonStatus.inProgress => const Icon(Icons.play_circle_outline),
        LessonStatus.done => const Icon(Icons.check_circle, color: Colors.green),
      },
      onTap: () {
        final builder = lesson.pageBuilder;
        if (lesson.status == LessonStatus.locked || builder == null) {
          // 门禁：未解锁的课只给提示。
          // ScaffoldMessenger 类比 iOS 里全局 toast/HUD 管理器。
          ScaffoldMessenger.of(context)
            ..hideCurrentSnackBar()
            ..showSnackBar(
              SnackBar(content: Text('先完成前面的课时，再解锁 ${lesson.id}')),
            );
          return;
        }
        // 类比 UINavigationController.pushViewController。
        Navigator.of(context).push(MaterialPageRoute(builder: builder));
      },
    );
  }
}
```

`lib/main.dart` 整体替换为：

```dart
import 'package:flutter/material.dart';

import 'home/lesson_list_page.dart';

void main() {
  runApp(const NativeLabApp());
}

class NativeLabApp extends StatelessWidget {
  const NativeLabApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'NativeLab 原生实验室',
      theme: ThemeData(colorSchemeSeed: Colors.indigo),
      home: const LessonListPage(),
    );
  }
}
```

同时删除 `test/widget_test.dart`。

- [ ] **Step 6: 跑测试确认通过 + 质量门**

```bash
cd native_lab/app && flutter test && flutter analyze
```

Expected: `All tests passed!`（3 个）+ `No issues found!`。

- [ ] **Step 7: 模拟器验证 + 截图给学员**

```bash
cd native_lab/app && flutter run
xcrun simctl io booted screenshot /tmp/l0_home.png
```

Expected: 首页 10 行课程，L0 带播放图标可点进解剖页，L1–L9 带锁；点锁定课弹 SnackBar。截图两张（列表页、L0 页）给学员确认。

---

### Task 3: 进度索引 + L0 课时文档

**Files:**
- Create: `native_lab/docs/lessons/README.md`
- Create: `native_lab/docs/lessons/L0-工程创建与原生工程解剖.md`

**Interfaces:**
- Consumes: Task 2 的代码结构（代码地图要写实际文件路径）
- Produces: 进度表（后续每课过关在此翻状态）；L0 课时文档

- [ ] **Step 1: 写进度索引**

`docs/lessons/README.md`：标题 + 一句话说明（进度真身在此，App 内列表是副本）+ 进度表。表格 3 列（课时/主题/状态），10 行照设计文档大纲，L0 状态 🔵 进行中，L1–L9 ⬜ 未开始。表下注明过关条件四项（模拟器跑通截图、analyze 0、全量 test 过、学员确认）。

- [ ] **Step 2: 写 L0 课时文档**

`docs/lessons/L0-工程创建与原生工程解剖.md`，按设计文档 §5 六段模板写，本课具体内容：

1. **本课要掌握什么**：ios/ 是完整 Xcode 工程、android/ 是完整 Gradle 工程，`flutter run` 只是对 xcodebuild/gradle 的包装；FlutterEngine 是 Dart 运行时、FlutterViewController 是它在 UIKit 里的容器；三条线程（UI/Platform/Raster），Platform 线程 = iOS 主线程（L1 讲 channel 回调线程的伏笔）。
2. **控件/API 速查表**：本课新出现的 Flutter 侧（MaterialApp、Scaffold、ListView.separated、ListTile、CircleAvatar、SnackBar/ScaffoldMessenger、Navigator/MaterialPageRoute、WidgetBuilder、switch 表达式）逐个给 iOS 类比 + 用法 + 坑；原生侧概念表（FlutterAppDelegate、FlutterViewController、Generated.xcconfig、GeneratedPluginRegistrant、FlutterActivity）同格式。
3. **代码地图**：Task 2 建的 5 个 Dart 文件各自职责，一句话一个。
4. **双端对照**：MainActivity.kt 与 AppDelegate.swift 的角色对照；AndroidManifest 与 Info.plist 对照。
5. **自测清单**：① 不查资料说出 Runner.xcworkspace 和 Runner.xcodeproj 该打开哪个、为什么 ② Dart 代码最终以什么形式进到 iOS 包里（App.framework）③ Platform 线程和 UI 线程各跑什么 ④ 为什么改 Info.plist 不需要动任何 Dart 代码。
6. **课后练习**：不用 `flutter run`，直接用 Xcode 打开 Runner.xcworkspace 选模拟器跑起来，观察 App 一样能启动——体会 flutter CLI 只是包装（提示：先跑过一次 flutter run 保证 Generated.xcconfig 存在）。

- [ ] **Step 3: 学员过自测清单**

把自测清单四道题发给学员口头过一遍，答不上的地方回文档补讲。此步骤完成的标志是学员说"过了"。

---

### Task 4: 过关翻牌 + 提交（等学员确认后才执行）

**Files:**
- Modify: `native_lab/docs/lessons/README.md`（L0 状态 🔵 → ✅）
- Modify: `native_lab/app/lib/lessons/lesson_registry.dart`（L0 status 改 `LessonStatus.done`）

**Interfaces:**
- Consumes: Task 3 的进度表；学员的口头过关确认
- Produces: L0 完成态的仓库快照，L1 开课的起点

- [ ] **Step 1: 翻状态**

README.md 进度表 L0 行改 ✅；lesson_registry.dart 里 L0 的 `status: LessonStatus.inProgress` 改为 `status: LessonStatus.done`。

- [ ] **Step 2: 全量质量门（改了代码就要重跑）**

```bash
cd native_lab/app && flutter analyze && flutter test
```

Expected: `No issues found!` + `All tests passed!`。

- [ ] **Step 3: 提交**

```bash
cd /Users/wenbo/Desktop/WBFlutter
git add native_lab/
git commit -m "L0 工程创建与原生工程解剖：课程列表骨架 + 进度索引 + 课时文档"
```

Expected: 一次提交包含 app 工程、docs 两份文档。

---

## Self-Review 记录

- 覆盖检查：设计文档中 L0 相关要求（工程创建、命名约定、课程列表点亮、进度表、课时文档模板六段、过关条件、git 约定）均有对应任务；L1–L9 明确留给各自的课前计划。
- 占位符检查：无 TBD；Task 3 的文档任务给出的是具体内容清单而非"写文档"空话。
- 一致性检查：`Lesson` 字段名（id/title/scenario/status/pageBuilder）、`lessonRegistry`、`NativeLabApp` 在测试与实现间一致；SnackBar 文案测试与实现一致（`先完成前面的课时，再解锁 L1`）。
