# L7 插件开发（抽出 nl_device_kit）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 L1 设备信息桥抽成独立 Flutter 插件包 `plugins/nl_device_kit`（官方模板三层结构），app 通过 `path:` 依赖并用新建的 L7 页消费它。

**Architecture:** 单包三层插件（`NlDeviceKit` 公开 API → `NlDeviceKitPlatform` 抽象接口 → `MethodChannelNlDeviceKit` 实现），原生 iOS/Android 各一个 `FlutterPlugin` 自动注册（对照前 6 课手写注册）。L1 手写版保持不动，L7 页并行对照。

**Tech Stack:** Flutter plugin 模板、`plugin_platform_interface`、MethodChannel、iOS Swift `FlutterPlugin`、Android Kotlin `FlutterPlugin`。

## Global Constraints

- 插件名 `nl_device_kit`，位置 `native_lab/plugins/nl_device_kit`，org `com.wenbo`，Android 包 `com.wenbo.nl_device_kit`。
- 插件 channel 名：`nl_device_kit`（独立于 L1 的 `com.wenbo.native_lab/device_info`）。
- 方法名（Dart↔原生逐字符一致）：`getDeviceInfo` / `getBatteryLevel` / `getSystemUpTime`。
- app path 依赖：`nl_device_kit: { path: ../plugins/nl_device_kit }`。
- 公开 API：`NlDeviceKit().getDeviceInfo() → Future<DeviceInfo>`、`getBatteryLevel() → Future<int>`、`getUptime() → Future<double>`。
- 不动已过关的 L1；不实现完整联邦多包；不发布 pub.dev；插件用手写 MethodChannel（不引 Pigeon）。
- 教学注释：iOS(Swift) 主讲 + Kotlin 对照，中文详注带 iOS 类比。
- 只 `git add native_lab/`；提交以 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` 收尾。
- 命令一律以 `cd /Users/wenbo/Desktop/WBFlutter/native_lab &&`（建插件/根操作）或 `cd .../native_lab/app &&`（app 操作）前缀。
- README 翻 ✅ 与 registry `done` 在过关四件套 + 学员口头确认后才做（Task 7）。

## File Structure

- `plugins/nl_device_kit/lib/nl_device_kit.dart` — 公开 API + `export src/device_info.dart`。
- `plugins/nl_device_kit/lib/nl_device_kit_platform_interface.dart` — 抽象接口（联邦接缝）。
- `plugins/nl_device_kit/lib/nl_device_kit_method_channel.dart` — MethodChannel 实现。
- `plugins/nl_device_kit/lib/src/device_info.dart` — 插件的 `DeviceInfo` 模型。
- `plugins/nl_device_kit/ios/Classes/NlDeviceKitPlugin.swift` — iOS 原生实现。
- `plugins/nl_device_kit/android/src/main/kotlin/com/wenbo/nl_device_kit/NlDeviceKitPlugin.kt` — Android 原生实现。
- `plugins/nl_device_kit/test/nl_device_kit_test.dart` — 插件测试。
- `app/lib/lessons/l7/l7_plugin_page.dart` — L7 页。
- 改：`app/pubspec.yaml`、`app/lib/lessons/lesson_registry.dart`、`app/test/lesson_list_test.dart`。
- 文档：`docs/lessons/L7-插件开发.md`、`docs/lessons/自测答案/L7-自测答案.md`、`docs/lessons/README.md`。

---

### Task 1: 脚手架插件包 + app path 依赖

**Files:**
- Create (via generator): `plugins/nl_device_kit/**`
- Modify: `app/pubspec.yaml`（dependencies 加 path 依赖）

**Interfaces:**
- Produces: 插件包 `nl_device_kit`（默认模板代码，Task 2 替换 API）。

- [ ] **Step 1: 用官方模板脚手架插件（本课知识点"plugin 模板"）**

Run:
```
cd /Users/wenbo/Desktop/WBFlutter/native_lab && flutter create --template=plugin --platforms=ios,android --org com.wenbo -i swift -a kotlin plugins/nl_device_kit
```
Expected: 生成 `plugins/nl_device_kit/`，含 `lib/`（三层）、`ios/`、`android/`、`example/`、`pubspec.yaml`。

- [ ] **Step 2: 删掉 example/（monorepo 保持精简，path 依赖插件不需要内嵌示例 app）**

Run:
```
cd /Users/wenbo/Desktop/WBFlutter/native_lab && rm -rf plugins/nl_device_kit/example
```

- [ ] **Step 3: 校验生成的 pubspec 插件块（org/package/pluginClass）**

Run: `cd /Users/wenbo/Desktop/WBFlutter/native_lab && grep -A8 "plugin:" plugins/nl_device_kit/pubspec.yaml`
Expected: `platforms:` 下 `ios: pluginClass: NlDeviceKitPlugin`、`android: package: com.wenbo.nl_device_kit / pluginClass: NlDeviceKitPlugin`。

- [ ] **Step 4: app 加 path 依赖并解析**

在 `app/pubspec.yaml` 的 `dependencies:` 下、`cupertino_icons` 之后加：
```yaml
  # L7：本地插件（企业"自家插件库"雏形，path 依赖）
  nl_device_kit:
    path: ../plugins/nl_device_kit
```
Run: `cd /Users/wenbo/Desktop/WBFlutter/native_lab/app && flutter pub get`
Expected: 成功，`nl_device_kit` 被解析为 path 依赖（无报错）。

- [ ] **Step 5: Commit**

```bash
cd /Users/wenbo/Desktop/WBFlutter/native_lab && git add plugins/nl_device_kit app/pubspec.yaml app/pubspec.lock && git commit -m "$(printf 'L7：脚手架 nl_device_kit 插件包 + app path 依赖\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>')"
```

---

### Task 2: 插件 Dart 三层 API + DeviceInfo 模型

**Files:**
- Create: `plugins/nl_device_kit/lib/src/device_info.dart`
- Overwrite: `plugins/nl_device_kit/lib/nl_device_kit_platform_interface.dart`
- Overwrite: `plugins/nl_device_kit/lib/nl_device_kit_method_channel.dart`
- Overwrite: `plugins/nl_device_kit/lib/nl_device_kit.dart`
- Overwrite: `plugins/nl_device_kit/test/nl_device_kit_test.dart`

**Interfaces:**
- Produces:
  - `class DeviceInfo { const DeviceInfo({required String model, systemName, systemVersion, appVersion}); factory DeviceInfo.fromMap(Map<String,Object?>); }`
  - `abstract class NlDeviceKitPlatform extends PlatformInterface { static NlDeviceKitPlatform instance; Future<DeviceInfo> getDeviceInfo(); Future<int> getBatteryLevel(); Future<double> getUptime(); }`
  - `class MethodChannelNlDeviceKit extends NlDeviceKitPlatform { final MethodChannel methodChannel = const MethodChannel('nl_device_kit'); }`
  - `class NlDeviceKit { Future<DeviceInfo> getDeviceInfo(); Future<int> getBatteryLevel(); Future<double> getUptime(); }`

- [ ] **Step 1: 写失败测试（覆盖生成的默认测试）**

`plugins/nl_device_kit/test/nl_device_kit_test.dart`:
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:nl_device_kit/nl_device_kit.dart';
import 'package:nl_device_kit/nl_device_kit_method_channel.dart';
import 'package:nl_device_kit/nl_device_kit_platform_interface.dart';
import 'package:plugin_platform_interface/plugin_platform_interface.dart';

// mock 掉 platform interface（这层缝的测试价值）：不碰真 channel，验证公开 API 转发。
class _MockPlatform extends NlDeviceKitPlatform with MockPlatformInterfaceMixin {
  @override
  Future<DeviceInfo> getDeviceInfo() async => const DeviceInfo(
      model: 'iPhone', systemName: 'iOS', systemVersion: '18.0', appVersion: '1.0');
  @override
  Future<int> getBatteryLevel() async => 88;
  @override
  Future<double> getUptime() async => 123.5;
}

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  test('NlDeviceKit 三方法经 platform interface 转发', () async {
    NlDeviceKitPlatform.instance = _MockPlatform();
    final kit = NlDeviceKit();
    expect((await kit.getDeviceInfo()).model, 'iPhone');
    expect(await kit.getBatteryLevel(), 88);
    expect(await kit.getUptime(), 123.5);
  });

  test('MethodChannel 实现：getDeviceInfo 把原生 Map 收成强类型', () async {
    final impl = MethodChannelNlDeviceKit();
    TestDefaultBinaryMessengerBinding.instance.defaultBinaryMessenger
        .setMockMethodCallHandler(impl.methodChannel, (call) async {
      if (call.method == 'getDeviceInfo') {
        return <String, Object?>{
          'model': 'Pixel', 'systemName': 'Android',
          'systemVersion': '14', 'appVersion': '2.0',
        };
      }
      return null;
    });
    addTearDown(() => TestDefaultBinaryMessengerBinding
        .instance.defaultBinaryMessenger
        .setMockMethodCallHandler(impl.methodChannel, null));

    final info = await impl.getDeviceInfo();
    expect(info.systemName, 'Android');
    expect(info.model, 'Pixel');
  });
}
```

- [ ] **Step 2: 运行看失败**

Run: `cd /Users/wenbo/Desktop/WBFlutter/native_lab/plugins/nl_device_kit && flutter test`
Expected: FAIL（`DeviceInfo`/新 API 未定义；生成的默认 `getPlatformVersion` 已被覆盖）。

- [ ] **Step 3a: 写 DeviceInfo 模型**

`plugins/nl_device_kit/lib/src/device_info.dart`:
```dart
/// 插件自己的设备信息强类型（与 app 内 L1 的 DeviceInfo 各自独立——并行对照）。
/// 教学点：插件对外只吐强类型，别让 Map 漏进消费方的业务代码。
class DeviceInfo {
  const DeviceInfo({
    required this.model,
    required this.systemName,
    required this.systemVersion,
    required this.appVersion,
  });

  factory DeviceInfo.fromMap(Map<String, Object?> map) => DeviceInfo(
        model: map['model'] as String? ?? 'unknown',
        systemName: map['systemName'] as String? ?? 'unknown',
        systemVersion: map['systemVersion'] as String? ?? 'unknown',
        appVersion: map['appVersion'] as String? ?? 'unknown',
      );

  final String model;
  final String systemName;
  final String systemVersion;
  final String appVersion;
}
```

- [ ] **Step 3b: 写 platform interface（联邦接缝）**

`plugins/nl_device_kit/lib/nl_device_kit_platform_interface.dart`:
```dart
import 'package:plugin_platform_interface/plugin_platform_interface.dart';

import 'nl_device_kit_method_channel.dart';
import 'src/device_info.dart';

/// 插件能力的【抽象接口】。为什么多这一层？为"同一 API、多种实现"留缝——
/// 换平台、换后端、或测试里塞 mock，都只替换 instance，公开 API 不变。
/// 这就是"联邦插件"的接缝（完整联邦会把它单独拆成一个包）。
abstract class NlDeviceKitPlatform extends PlatformInterface {
  NlDeviceKitPlatform() : super(token: _token);

  static final Object _token = Object();
  static NlDeviceKitPlatform _instance = MethodChannelNlDeviceKit();

  /// 默认实现是 MethodChannel 版；测试可换成 mock（见 test）。
  static NlDeviceKitPlatform get instance => _instance;
  static set instance(NlDeviceKitPlatform instance) {
    PlatformInterface.verifyToken(instance, _token); // 防止外部乱塞非法实现
    _instance = instance;
  }

  Future<DeviceInfo> getDeviceInfo() => throw UnimplementedError();
  Future<int> getBatteryLevel() => throw UnimplementedError();
  Future<double> getUptime() => throw UnimplementedError();
}
```

- [ ] **Step 3c: 写 MethodChannel 实现**

`plugins/nl_device_kit/lib/nl_device_kit_method_channel.dart`:
```dart
import 'package:flutter/foundation.dart';
import 'package:flutter/services.dart';

import 'nl_device_kit_platform_interface.dart';
import 'src/device_info.dart';

/// 默认平台实现：走一条插件专属 channel `nl_device_kit`（独立于 L1 的手写 channel）。
/// 对照 L1 的 DeviceInfoBridge——同样是 invokeMethod + 收 Map，只是现在住在插件里，
/// 原生端由 GeneratedPluginRegistrant 自动注册（不再手写注册）。
class MethodChannelNlDeviceKit extends NlDeviceKitPlatform {
  @visibleForTesting
  final MethodChannel methodChannel = const MethodChannel('nl_device_kit');

  @override
  Future<DeviceInfo> getDeviceInfo() async {
    final map = await methodChannel.invokeMapMethod<String, Object?>('getDeviceInfo');
    if (map == null) {
      throw PlatformException(code: 'NULL_RESULT', message: '原生侧返回了空数据');
    }
    return DeviceInfo.fromMap(map);
  }

  @override
  Future<int> getBatteryLevel() async {
    final level = await methodChannel.invokeMethod<int>('getBatteryLevel');
    return level ?? -1;
  }

  @override
  Future<double> getUptime() async {
    final time = await methodChannel.invokeMethod<double>('getSystemUpTime');
    return time ?? 0;
  }
}
```

- [ ] **Step 3d: 写公开 API**

`plugins/nl_device_kit/lib/nl_device_kit.dart`:
```dart
import 'nl_device_kit_platform_interface.dart';
import 'src/device_info.dart';

export 'src/device_info.dart'; // 消费方 import 一个库即可拿到 DeviceInfo

/// 插件对外的门面：消费方只认这个类，不关心底层是 channel 还是别的实现。
/// 三方法都委托给 NlDeviceKitPlatform.instance（默认 MethodChannel 版）。
class NlDeviceKit {
  Future<DeviceInfo> getDeviceInfo() =>
      NlDeviceKitPlatform.instance.getDeviceInfo();
  Future<int> getBatteryLevel() =>
      NlDeviceKitPlatform.instance.getBatteryLevel();
  Future<double> getUptime() => NlDeviceKitPlatform.instance.getUptime();
}
```

- [ ] **Step 4: 运行看通过 + analyze**

Run: `cd /Users/wenbo/Desktop/WBFlutter/native_lab/plugins/nl_device_kit && flutter test && flutter analyze`
Expected: 2 tests PASS，analyze `No issues found!`。

- [ ] **Step 5: Commit**

```bash
cd /Users/wenbo/Desktop/WBFlutter/native_lab && git add plugins/nl_device_kit/lib plugins/nl_device_kit/test && git commit -m "$(printf 'L7：插件 Dart 三层 API + DeviceInfo 模型\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>')"
```

---

### Task 3: 插件 iOS 原生实现（Swift FlutterPlugin）

**Files:**
- Overwrite: `plugins/nl_device_kit/ios/Classes/NlDeviceKitPlugin.swift`

**Interfaces:**
- Consumes: channel `nl_device_kit`、方法名 `getDeviceInfo`/`getBatteryLevel`/`getSystemUpTime`（Task 2 契约）。

> 无法单测（原生）。验证 = app iOS 模拟器编译 + 截图（Task 7）。

- [ ] **Step 1: 写 iOS 插件实现（搬 L1 DeviceInfoBridge 逻辑）**

`plugins/nl_device_kit/ios/Classes/NlDeviceKitPlugin.swift`:
```swift
import Flutter
import UIKit

/// 插件的 iOS 入口。对照前 6 课：那些桥在 AppDelegate 里【手写注册】；
/// 插件实现 FlutterPlugin，由 GeneratedPluginRegistrant 【自动】调 register(with:)——
/// 这是"插件"与"app 内嵌桥"最大的工程差异。
public class NlDeviceKitPlugin: NSObject, FlutterPlugin {
  public static func register(with registrar: FlutterPluginRegistrar) {
    let channel = FlutterMethodChannel(name: "nl_device_kit", binaryMessenger: registrar.messenger())
    let instance = NlDeviceKitPlugin()
    registrar.addMethodCallDelegate(instance, channel: channel)
  }

  public func handle(_ call: FlutterMethodCall, result: @escaping FlutterResult) {
    switch call.method {
    case "getDeviceInfo":
      let device = UIDevice.current
      let appVersion =
        Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String ?? "unknown"
      result([
        "model": device.model,
        "systemName": device.systemName,
        "systemVersion": device.systemVersion,
        "appVersion": appVersion,
      ])
    case "getBatteryLevel":
      let device = UIDevice.current
      device.isBatteryMonitoringEnabled = true
      let level = device.batteryLevel
      if level < 0 {
        result(FlutterError(code: "UNAVAILABLE", message: "拿不到电量（模拟器没有电池）", details: nil))
      } else {
        result(Int(level * 100))
      }
    case "getSystemUpTime":
      result(ProcessInfo.processInfo.systemUptime)
    default:
      result(FlutterMethodNotImplemented)
    }
  }
}
```

- [ ] **Step 2: 校验 app 侧能解析插件 iOS（config-only 刷新）**

Run: `cd /Users/wenbo/Desktop/WBFlutter/native_lab/app && flutter pub get && flutter build ios --config-only`
Expected: 无 pod/插件解析报错（真正编译在 Task 7）。

- [ ] **Step 3: Commit**

```bash
cd /Users/wenbo/Desktop/WBFlutter/native_lab && git add plugins/nl_device_kit/ios && git commit -m "$(printf 'L7：插件 iOS 实现（Swift FlutterPlugin 自动注册）\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>')"
```

---

### Task 4: 插件 Android 原生实现（Kotlin FlutterPlugin）

**Files:**
- Overwrite: `plugins/nl_device_kit/android/src/main/kotlin/com/wenbo/nl_device_kit/NlDeviceKitPlugin.kt`

**Interfaces:**
- Consumes: channel `nl_device_kit`、三方法名（Task 2 契约）。

> 按惯例 Android 不编译，只保证与 iOS 概念对齐、代码正确。

- [ ] **Step 1: 写 Android 插件实现（搬 L1 handler 逻辑）**

`plugins/nl_device_kit/android/src/main/kotlin/com/wenbo/nl_device_kit/NlDeviceKitPlugin.kt`:
```kotlin
package com.wenbo.nl_device_kit

import android.content.Context
import android.os.BatteryManager
import android.os.Build
import android.os.SystemClock
import io.flutter.embedding.engine.plugins.FlutterPlugin
import io.flutter.plugin.common.MethodCall
import io.flutter.plugin.common.MethodChannel
import io.flutter.plugin.common.MethodChannel.MethodCallHandler
import io.flutter.plugin.common.MethodChannel.Result

/// 插件的 Android 入口。对照前 6 课在 MainActivity 里【手写注册】：
/// 这里实现 FlutterPlugin，onAttachedToEngine 里挂 channel，由框架【自动】调用。
class NlDeviceKitPlugin : FlutterPlugin, MethodCallHandler {
  private lateinit var channel: MethodChannel
  private lateinit var context: Context

  override fun onAttachedToEngine(binding: FlutterPlugin.FlutterPluginBinding) {
    channel = MethodChannel(binding.binaryMessenger, "nl_device_kit")
    channel.setMethodCallHandler(this)
    context = binding.applicationContext
  }

  override fun onMethodCall(call: MethodCall, result: Result) {
    when (call.method) {
      "getDeviceInfo" -> result.success(
        mapOf(
          "model" to Build.MODEL,
          "systemName" to "Android",
          "systemVersion" to Build.VERSION.RELEASE,
          "appVersion" to (context.packageManager
            .getPackageInfo(context.packageName, 0).versionName ?: "unknown"),
        )
      )
      "getBatteryLevel" -> {
        val bm = context.getSystemService(Context.BATTERY_SERVICE) as BatteryManager
        val level = bm.getIntProperty(BatteryManager.BATTERY_PROPERTY_CAPACITY)
        if (level < 0) result.error("UNAVAILABLE", "拿不到电量", null)
        else result.success(level)
      }
      "getSystemUpTime" -> result.success(SystemClock.elapsedRealtime() / 1000.0)
      else -> result.notImplemented()
    }
  }

  override fun onDetachedFromEngine(binding: FlutterPlugin.FlutterPluginBinding) {
    channel.setMethodCallHandler(null)
  }
}
```

- [ ] **Step 2: 静态校验**

人工核对：包名 `com.wenbo.nl_device_kit` 与 pubspec `package` 一致；channel 名、三方法名与 iOS/Dart 逐字符一致。

- [ ] **Step 3: Commit**

```bash
cd /Users/wenbo/Desktop/WBFlutter/native_lab && git add plugins/nl_device_kit/android && git commit -m "$(printf 'L7：插件 Android 实现（Kotlin FlutterPlugin 自动注册）\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>')"
```

---

### Task 5: L7 页 + 注册表上架 + 锁定测试顺移

**Files:**
- Create: `app/lib/lessons/l7/l7_plugin_page.dart`
- Create: `app/test/l7_page_test.dart`
- Modify: `app/lib/lessons/lesson_registry.dart`
- Modify: `app/test/lesson_list_test.dart`

**Interfaces:**
- Consumes: `NlDeviceKit`、`DeviceInfo`（Task 2）。
- Produces: `class L7PluginPage extends StatefulWidget { const L7PluginPage({Key? key}); }`

- [ ] **Step 1: 写失败测试（页面冒烟 + 锁定顺移）**

改 `app/test/lesson_list_test.dart` 的锁定 test（样本 L7→L8）：
```dart
  testWidgets('点击锁定课时只弹提示，不跳转', (tester) async {
    await tester.pumpWidget(const NativeLabApp());
    // L7 已解锁，锁定样本换成 L8。
    await tester.tap(find.text('add-to-app：原生工程接入 Flutter'));
    await tester.pump();
    expect(find.text('先完成前面的课时，再解锁 L8'), findsOneWidget);
  });
```

新增 `app/test/l7_page_test.dart`:
```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:native_lab/lessons/l7/l7_plugin_page.dart';

void main() {
  testWidgets('L7 页面能 build 并显示标题', (tester) async {
    await tester.pumpWidget(const MaterialApp(home: L7PluginPage()));
    expect(find.text('L7 插件：nl_device_kit'), findsOneWidget);
  });
}
```

- [ ] **Step 2: 运行看失败**

Run: `cd /Users/wenbo/Desktop/WBFlutter/native_lab/app && flutter test test/l7_page_test.dart test/lesson_list_test.dart`
Expected: FAIL（`l7_plugin_page.dart` 不存在；lesson_list_test 里 L8 仍 locked 但样本文案未改前会失败前置）。

- [ ] **Step 3a: 写 L7 页**

`app/lib/lessons/l7/l7_plugin_page.dart`:
```dart
import 'package:flutter/material.dart';
import 'package:nl_device_kit/nl_device_kit.dart';

/// L7 入口页：用插件 nl_device_kit 取设备信息。
/// 对照 L1：L1 的桥散在 app 里（手写 channel + AppDelegate 手写注册）；
/// 这里同样的能力来自【独立插件】——app 只 import 一个包、调 NlDeviceKit()，
/// 原生注册由 GeneratedPluginRegistrant 自动完成。
class L7PluginPage extends StatefulWidget {
  const L7PluginPage({super.key});

  @override
  State<L7PluginPage> createState() => _L7PluginPageState();
}

class _L7PluginPageState extends State<L7PluginPage> {
  final _kit = NlDeviceKit();
  DeviceInfo? _info;
  int? _battery;
  double? _uptime;
  Object? _error;

  Future<void> _load() async {
    try {
      final info = await _kit.getDeviceInfo();
      final battery = await _kit.getBatteryLevel();
      final uptime = await _kit.getUptime();
      setState(() {
        _info = info;
        _battery = battery;
        _uptime = uptime;
        _error = null;
      });
    } catch (e) {
      setState(() => _error = e);
    }
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('L7 插件：nl_device_kit')),
      body: ListView(
        padding: const EdgeInsets.all(16),
        children: [
          Card(
            color: Theme.of(context).colorScheme.surfaceContainerHighest,
            child: const Padding(
              padding: EdgeInsets.all(16),
              child: Text(
                '和 L1 是同一份设备信息，但这里来自【独立插件】：app 只 import nl_device_kit、'
                '调 NlDeviceKit()，原生端由 GeneratedPluginRegistrant 自动注册（对照 L1 的手写注册）。',
              ),
            ),
          ),
          const SizedBox(height: 16),
          FilledButton.icon(
            onPressed: _load,
            icon: const Icon(Icons.extension),
            label: const Text('用插件读取设备信息'),
          ),
          const SizedBox(height: 12),
          if (_error != null)
            Card(
              color: Theme.of(context).colorScheme.errorContainer,
              child: Padding(
                padding: const EdgeInsets.all(16),
                child: Text('读取失败：$_error'),
              ),
            )
          else if (_info != null)
            Card(
              child: Column(
                children: [
                  _row('机型', _info!.model),
                  _row('系统', '${_info!.systemName} ${_info!.systemVersion}'),
                  _row('App 版本', _info!.appVersion),
                  _row('电量', _battery == null ? '未知' : '$_battery%'),
                  _row('开机时长', _uptime == null ? '未知' : '${_uptime!.toStringAsFixed(0)}s'),
                ],
              ),
            ),
        ],
      ),
    );
  }

  Widget _row(String k, String v) => ListTile(
        dense: true,
        title: Text(k),
        trailing: Text(v, style: const TextStyle(fontWeight: FontWeight.bold)),
      );
}
```

- [ ] **Step 3b: 注册表上架**

改 `app/lib/lessons/lesson_registry.dart`：顶部加 `import 'l7/l7_plugin_page.dart';`；把 L7 那条从 `const Lesson(... status: LessonStatus.locked,)` 改成：
```dart
  Lesson(
    id: 'L7',
    title: '插件开发',
    scenario: '把设备信息桥抽成独立 plugin 包',
    status: LessonStatus.inProgress,
    pageBuilder: (_) => const L7PluginPage(),
  ),
```

- [ ] **Step 4: 运行看通过 + analyze**

Run: `cd /Users/wenbo/Desktop/WBFlutter/native_lab/app && flutter test test/l7_page_test.dart test/lesson_list_test.dart && flutter analyze lib/lessons/l7`
Expected: 全 PASS，analyze `No issues found!`。

- [ ] **Step 5: Commit**

```bash
cd /Users/wenbo/Desktop/WBFlutter/native_lab && git add app/lib/lessons/l7 app/lib/lessons/lesson_registry.dart app/test/l7_page_test.dart app/test/lesson_list_test.dart && git commit -m "$(printf 'L7：页面 + 注册表上架 + 锁定测试顺移到 L8\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>')"
```

---

### Task 6: 课程文档（L7 讲义 + 自测答案）

**Files:**
- Create: `docs/lessons/L7-插件开发.md`
- Create: `docs/lessons/自测答案/L7-自测答案.md`

**Interfaces:** 无代码接口。文档须在 Task 7 验收前存在（硬规则）。

- [ ] **Step 1: 写 L7 讲义（7 段式，沿用 L5/L6 结构）**

`docs/lessons/L7-插件开发.md`，7 段：① 本课掌握什么（插件 = 把能力打包成可复用包；自动注册 vs 手写注册）；② API/结构速查表（`flutter create --template=plugin`、三层文件职责、`PlatformInterface`、`MockPlatformInterfaceMixin`、path 依赖语法、`FlutterPlugin.register(with:)`/`onAttachedToEngine`）；③ 代码地图（插件三层 + model + iOS/Android + app path 依赖 + L7 页）；④ 测试（mock platform interface 测公开 API + mock channel 测实现 + 页面冒烟）；⑤ 双端对照（iOS `FlutterPlugin.register(with:)` + `addMethodCallDelegate` vs Android `onAttachedToEngine` + `setMethodCallHandler`；自动注册机制）；⑥ 自测清单（插件 channel 为何要独立于 L1？自动注册怎么发生的？platform-interface 这层缝解决什么/怎么帮测试？path 依赖 vs pub.dev 依赖？联邦插件是什么、何时才需要多包？）；⑦ 课后练习（给插件加 `getDeviceModelName()`，走 interface→实现→两端→暴露→调用→测试全回路）。

- [ ] **Step 2: 写自测答案**

`docs/lessons/自测答案/L7-自测答案.md`：逐条回答 ⑥。要点：独立 channel 避免与 L1 手写注册撞名；自动注册 = GeneratedPluginRegistrant 在引擎起来时调用每个插件的 register；platform interface 让"同一 API 多实现/可 mock"，测试用 `MockPlatformInterfaceMixin` 绕过 token；path 用于本地开发、pub.dev/git 用于分发；联邦 = 把接口与各平台实现拆成多包，仅当需要第三方独立提供某平台实现时才值得。

- [ ] **Step 3: Commit**

```bash
cd /Users/wenbo/Desktop/WBFlutter/native_lab && git add docs/lessons/L7-插件开发.md "docs/lessons/自测答案/L7-自测答案.md" && git commit -m "$(printf 'L7 讲义 + 自测答案\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>')"
```

---

### Task 7: 过关验收 + README 翻 ✅

**Files:**
- Modify: `docs/lessons/README.md`（L7 ⬜→✅）
- Modify: `app/lib/lessons/lesson_registry.dart`（L7 inProgress→done）

- [ ] **Step 1: 全量校验（含插件包）**

Run:
```
cd /Users/wenbo/Desktop/WBFlutter/native_lab/app && flutter analyze && flutter test
cd /Users/wenbo/Desktop/WBFlutter/native_lab/plugins/nl_device_kit && flutter test
```
Expected: app analyze `No issues found!`、app test 全过、插件 test 全过。

- [ ] **Step 2: 模拟器实跑截图**

`cd app && flutter pub get`，用 arm64-only 方案（memory `env-xcode27-lipo-simulator`）在 iPhone 模拟器编译运行，进 L7：点"用插件读取设备信息"，确认机型/系统/App 版本/开机时长显示（电量在模拟器为未知/报错兜底）。截图留证。这一步同时验证**插件原生代码被自动注册**（无手写注册却能通）。

- [ ] **Step 3: 学员口头确认**

请学员过 L7 讲义 ⑥ 自测清单并口头确认过关。**未确认不翻状态。**

- [ ] **Step 4: 翻状态**

- `docs/lessons/README.md` L7 行 → `| [L7](L7-插件开发.md) | 插件开发 | ✅ 已完成 |`。
- `lesson_registry.dart` L7 `status: LessonStatus.inProgress` → `LessonStatus.done`。

- [ ] **Step 5: Commit**

```bash
cd /Users/wenbo/Desktop/WBFlutter/native_lab && git add app/lib/lessons/lesson_registry.dart docs/lessons/README.md && git commit -m "$(printf 'NativeLab L7 完成：插件开发（抽出 nl_device_kit）\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>')"
```

---

## Self-Review

**Spec coverage：**
- 插件三层结构（②）→ Task 1（脚手架）+ Task 2（三层 + model）。✅
- 并行保留 L1、新建 L7 页 → Task 5；L1 不改（全程无 L1 文件改动）。✅
- 独立 channel `nl_device_kit` → Global Constraints + Task 2/3/4。✅
- 自动注册 vs 手写注册 → Task 3/4 注释 + Task 6 讲义 ⑤。✅
- path 依赖 → Task 1 Step 4 + 讲义。✅
- platform-interface 接缝 + 联邦概念 → Task 2（interface）+ Task 6 讲义 ⑥。✅
- 测试（mock interface + mock channel + 页面冒烟 + 锁定顺移）→ Task 2、Task 5。✅
- 课后练习（getDeviceModelName 全回路）→ Task 6 ⑦。✅
- 进度 plumbing → Task 5 + Task 7。✅

**Placeholder scan：** 无 TBD/TODO；每个代码步骤含完整代码（Task 6 讲义为大纲式，属文档非代码，可接受）。✅

**Type consistency：** `NlDeviceKit`（getDeviceInfo/getBatteryLevel/getUptime）、`NlDeviceKitPlatform`、`MethodChannelNlDeviceKit.methodChannel`、`DeviceInfo`（model/systemName/systemVersion/appVersion）、channel `nl_device_kit`、方法名 `getDeviceInfo`/`getBatteryLevel`/`getSystemUpTime`、包 `com.wenbo.nl_device_kit` 在 Task 2/3/4/5 全对齐。✅

> 注：Dart 侧公开方法名 `getUptime()`，其底层 channel 方法名是 `getSystemUpTime`（与 L1 及两端原生一致）——两者不同是有意的（Dart API 命名 vs 通道协议名），已在 method_channel 实现里对齐。
